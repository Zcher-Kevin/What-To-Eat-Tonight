# Database Configuration
# For local development (each developer's own database)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=what_to_eat_db
DB_USER=what_to_eat_user    # Use a project-specific user instead of root
DB_PASSWORD=your_password_here

# For connecting to a shared development database
# DB_HOST=team-database-ip-or-hostname
# DB_PORT=3306
# DB_NAME=what_to_eat_db
# DB_USER=team_member_username
# DB_PASSWORD=team_password

# Server Configuration
PORT=3000
